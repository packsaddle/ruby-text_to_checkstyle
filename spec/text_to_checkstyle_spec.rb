require 'spec_helper'

describe TextToCheckstyle do
  it 'has a version number' do
    expect(TextToCheckstyle::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
